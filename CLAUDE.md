# Nivello Claude Instructions

Use the project skills in `.claude/skills` before creating or changing app pages.

For Next.js pages, use `.claude/skills/next-page-pattern/SKILL.md`.

Route folder names must always be in English. Keep visible product copy in pt-BR when the UI is for users.

The Next.js app lives in `project/`.
