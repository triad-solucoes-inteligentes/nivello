# Caddy reverse proxy — terminates TLS and forwards to the app container.
# Automatic HTTPS via Let's Encrypt: just needs {$DOMAIN} to resolve to this
# VM's public IP. Certs are stored in the caddy_data volume.

{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode zstd gzip
	reverse_proxy app:3000
}
